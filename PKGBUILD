pkgname=wrapass
pkgver=0.1
pkgrel=1
pkgdesc="wraps ssh and in the future git for pass password manager integration"
# ssh git ?
depends=('python3')
source=("wrapass.py"
        "passh"
        "pascp")
md5sums=()

package() {
    #install
}
